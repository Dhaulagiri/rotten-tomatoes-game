<div class="media">
  <div class="media-left">
    <a href="#">
      <img class="media-object" {{bind-attr src=round.movie.thumbnail alt=round.movie.title}}>
    </a>
  </div>
  <div class="media-body">
    <h4 class="media-heading">{{round.movie.title}}</h4>
    {{input value=round.guess}}
  </div>
</div>

{{yield}}
